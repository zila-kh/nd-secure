{
  "identifier": "default",
  "description": "Minimal WebView permissions for ND Secure; secrets and clipboard access stay behind Rust commands",
  "windows": ["main"],
  "permissions": [
    "dialog:allow-open",
    "dialog:allow-message"
  ]
}
